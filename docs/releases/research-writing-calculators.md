# Research, Writing & Calculators

Research uses the existing Web Search request with a separate panel for source links actually returned by the provider. Links are not independently verified facts; missing citations are shown explicitly. Writing preserves up to eight session alternatives, each with its own saved chat, copy and download actions. Download comparisons before leaving the tool.

The calculator evaluates a restricted arithmetic grammar locally with no model call or credit charge. It supports basic operators, powers, parentheses and scientific notation with bounded complexity. Percent means remainder. Results use floating-point arithmetic, not arbitrary-precision decimal math. Undefined, overflow and unsafe-range results are rejected.

Generation uses the normal explicit request, credit reservation and settlement path. Failed results preserve available text and known receipts; missing receipts are labelled unknown. The film uses illustrative task examples.
